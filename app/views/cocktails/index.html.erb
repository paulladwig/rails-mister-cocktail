<h1>Cocktails</h1>

<% @cocktails.each do |cocktail| %>
  <div class="card">
      <%=link_to cocktail.name, cocktail_path(cocktail) %>
  </div>
<% end %>

<h5>Add a Cocktail</h5>
<%= simple_form_for(@cocktail) do |f| %>
  <%= f.input :name %>
  <%= f.submit "Add"%>
<% end %>
